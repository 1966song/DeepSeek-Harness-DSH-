/**
 * Preload bridge: currently unused — the window controls are the native
 * Window Controls Overlay, so the page needs no shell API. Kept as a seam
 * for future IPC (e.g. server status, deep links).
 */
