{
  "name": "dsh-plugin-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin suite for DeepSeek Harness (DSH): wallpaper, usage, prompt rail, desktop shell",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-all.mjs",
    "typecheck": "pnpm -r run typecheck",
    "check": "pnpm -r run check"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
